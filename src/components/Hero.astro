---
import { siteConfig } from "../lib/site-config";
---

<section class="section-shell grid grid-cols-1 lg:grid-cols-2 gap-12 items-center" id="top">
  <div class="flex flex-col gap-6">
    <span class="badge w-fit">New • Production-ready Astro theme</span>
    <h1
      class="font-display text-4xl sm:text-5xl lg:text-6xl font-semibold text-white leading-tight"
    >
      Launch brighter products with <span class="text-aurora">{siteConfig.name}</span>
    </h1>
    <p class="text-lg text-slate leading-relaxed max-w-2xl">
      {siteConfig.description}
    </p>
    <div class="flex flex-wrap items-center gap-3">
      <a class="button-primary" href={siteConfig.cta.primary.href}>{siteConfig.cta.primary.label}</a
      >
      <a class="button-ghost" href={siteConfig.cta.secondary.href}
        >{siteConfig.cta.secondary.label}</a
      >
    </div>
    <div class="grid grid-cols-2 gap-4 mt-4">
      {
        siteConfig.highlights.map((highlight) => (
          <div class="card-surface p-4 flex gap-3 items-start">
            <div class="icon-circle">✨</div>
            <p class="text-sm text-white leading-snug">{highlight}</p>
          </div>
        ))
      }
    </div>
  </div>
  <div class="relative">
    <div class="absolute inset-0 grid-fade opacity-60" aria-hidden="true"></div>
    <div class="relative card-surface p-8 flex flex-col gap-6">
      <p class="text-xs uppercase text-aurora font-semibold tracking-wide">Dashboard preview</p>
      <div
        class="aspect-[4/3] w-full rounded-xl bg-gradient-to-br from-white/5 to-white/10 border border-white/10 p-6 flex flex-col gap-4"
      >
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-slate">Active sessions</p>
            <p class="text-3xl font-semibold text-white">2,318</p>
          </div>
          <span class="badge">+12% this week</span>
        </div>
        <div class="grid grid-cols-3 gap-3 text-sm text-slate">
          <div class="card-surface p-4">
            <p>Deploys</p>
            <p class="text-2xl font-semibold text-white">42</p>
          </div>
          <div class="card-surface p-4">
            <p>Uptime</p>
            <p class="text-2xl font-semibold text-white">99.9%</p>
          </div>
          <div class="card-surface p-4">
            <p>Regions</p>
            <p class="text-2xl font-semibold text-white">12</p>
          </div>
        </div>
        <div class="card-surface p-4 flex items-center justify-between">
          <div class="flex items-center gap-3">
            <div class="icon-circle">🌙</div>
            <div>
              <p class="text-white font-semibold leading-none">Dark mode native</p>
              <p class="text-xs text-slate">Crafted for OLED and night readers</p>
            </div>
          </div>
          <button class="button-primary text-sm px-4 py-2">Preview</button>
        </div>
      </div>
    </div>
  </div>
</section>
