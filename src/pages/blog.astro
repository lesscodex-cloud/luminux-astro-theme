---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Blog — Luminux" description="Read the latest updates from the Luminux team.">
  <section class="section-shell flex flex-col gap-6">
    <span class="badge w-fit">Blog</span>
    <h1 class="font-display text-4xl font-semibold text-white">Notes from the build log</h1>
    <p class="text-lg text-slate max-w-2xl">
      Keep this page as a placeholder until you are ready to publish product updates, tutorials, and release notes.
    </p>
    <div class="grid gap-4 md:grid-cols-2">
      {[1, 2, 3, 4].map((post) => (
        <article class="card-surface p-6 flex flex-col gap-3" aria-label={`Placeholder post ${post}`}>
          <p class="text-xs uppercase tracking-wide text-aurora">Category</p>
          <h2 class="text-xl font-semibold text-white">Post headline {post}</h2>
          <p class="text-slate">Swap this copy with a short summary that invites visitors to read more.</p>
          <a class="button-ghost w-fit" href="#">Read post</a>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
